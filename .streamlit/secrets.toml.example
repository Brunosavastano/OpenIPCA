# OpenIPCA — example Streamlit secrets file.
# Copy to ".streamlit/secrets.toml" and fill in your own values.
# NEVER commit the real "secrets.toml" (it is git-ignored).

OPENIPCA_AI_ENABLED = false
OPENIPCA_AI_PROVIDER = "none"
ANTHROPIC_API_KEY = ""
OPENAI_API_KEY = ""

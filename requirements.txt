numpy>=1.26
pandas>=2.2
plotly>=5.22
pyarrow>=15.0
PyYAML>=6.0
requests>=2.31
streamlit>=1.36
# AI provider for the public "Ask the IPCA" demo. The app is model-agnostic; Gemini
# (free tier) is the one hosted provider the deploy needs. Without it the live AI
# silently degrades to the pre-generated replay.
google-generativeai>=0.8
pytest>=8.0

